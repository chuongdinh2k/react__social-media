import { ChatContent } from "@components";
import React from "react";

import { AppTopBar } from "@components";

export const Chat = () => {
    return (
        <>
            <AppTopBar />
            <ChatContent />
        </>
    );
};
